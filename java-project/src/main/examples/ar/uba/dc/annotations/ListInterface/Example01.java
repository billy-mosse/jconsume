package ar.uba.dc.annotations.ListInterface;

import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

public class Example01 {
	/*
	public static IAnimal animal;
	public static void main(String[] args){
		int i = Integer.parseInt(args[0]);
		List<Integer> myList = new ArrayList<Integer>();
		doSomething(animal);
	}
	
	public static void doSomething(IAnimal animal)
	{
		animal.jump();
	}
	
	//int o Integer?
	//Deberia tener un summary para iterator, y otro para next.
	public static List copy(Iterable src)
	{
		//This is because these nodes could be resolved
		//when more context is available, at which point we still need to
		//apply the effect of the non-analyzable call to those node
		
		List l = new ArrayList<>();
		Iterator it = src.iterator();
		while(it.hasNext())
		{
			Integer i = (Integer) it.next();
			l.add(i);
		}
		return l;
	}*/
}
